# 项目目标提示词

将以下内容作为后续开发本项目时的任务提示词使用：

```text
你正在开发“厦门麻将 AI”项目。项目目标是构建一个规则可验证、决策可解释、可持续训练迭代的厦门麻将 AI：先提供可信的人机对战网页，再逐步形成可用于监督学习、DAgger 和 hybrid 推理的教师数据与模型能力。

当前基线：仓库已经实现“厦门麻将核心金牌版”的 Python 规则引擎和本地网页桌。它支持 4 人对局、花牌自动补牌、吃/碰/明杠/暗杠/补杠、翻金与金牌万能牌、标准和与七对；浏览器端必须始终隐藏 AI 暗牌和牌墙顺序。默认对手是可解释的规则 Teacher AI，而非伪装成已训练好的神经网络。

开发优先级：
1. 规则正确性优先。新增玩法或计分前，先明确规则开关、边界条件和测试；厦门地方差异不能凭空假定。
2. 保持网页可玩。人类玩家能完成一整局，AI 能合法行动，结算清晰；服务默认本机运行，不暴露敏感游戏状态。
3. 为训练闭环铺路。设计稳定的状态/动作编码、教师轨迹导出、回放与评测接口；遵循“规则引擎 → Teacher → 监督预训练 → DAgger → hybrid 推理”的路线。
4. 所有模型或策略改动都应与 Teacher 基线比较，使用固定种子、换座或配对对局降低随机性；不要只报告单局胜负。
5. 代码保持轻量、模块化、可测试。修改后运行相关单元测试；网页交互变更还要做真实浏览器验证，而非只调用后端 API。

本次工作前先阅读 README、现有测试与规则实现；不要重写无关代码，不要提交运行缓存、浏览器快照或密钥。完成后简要说明：实现内容、规则/训练影响、验证方式与仍待确认的本地规则。
```

## 近期里程碑

1. 完善核心金牌版规则、计分规则开关和网页操作反馈。
2. 导出 Teacher 对局轨迹，建立可复现实验集与离线评测。
3. 实现监督学习基线，并以 DAgger 处理策略分布偏移。
4. 将模型评分与规则 Teacher、风险控制和进度策略组合为 hybrid 推理。
5. 在取得本地确认的厦门规则后，扩展游金、三金倒、跟打及底/水制等差异化玩法。
